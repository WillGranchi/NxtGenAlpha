# npm configuration for Railway deployment
# This file attempts to suppress deprecation warnings during build
# Note: Railway's build process may still show warnings as it uses its own npm commands

legacy-peer-deps=false

